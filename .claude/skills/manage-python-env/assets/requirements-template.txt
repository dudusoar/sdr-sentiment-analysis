# Core Dependencies
# Pinned versions for production
requests==2.28.2
pandas==1.5.3
numpy==1.24.3
scikit-learn==1.3.0
matplotlib==3.7.2
seaborn==0.12.2

# Database
sqlalchemy==2.0.19
psycopg2-binary==2.9.9
redis==5.0.1

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Development Dependencies (not for production)
# These are typically in requirements-dev.txt or pyproject.toml

# Testing
pytest==7.4.3
pytest-cov==4.1.0
pytest-xdist==3.5.0
pytest-mock==3.12.0

# Code Quality
black==23.11.0
flake8==6.1.0
mypy==1.7.1
isort==5.12.0
pre-commit==3.5.0

# Documentation
sphinx==7.2.6
sphinx-rtd-theme==1.3.0
myst-parser==2.0.0

# Optional: Type stubs for better mypy checking
types-requests==2.31.0.10
types-python-dateutil==2.8.19.14

# Environment-specific packages
# Use environment markers for conditional dependencies

# Windows specific (if needed)
# pywin32==306; sys_platform == 'win32'

# Linux specific (if needed)
# systemd-python==235; sys_platform == 'linux'

# Version constraints examples:
# >= for minimum version
# <= for maximum version
# ~= for compatible version
# != to exclude specific version

# Example constraints:
# package>=1.0.0,<2.0.0  # Any 1.x version
# package~=1.2.0         # >=1.2.0,<1.3.0
# package!=1.2.3         # Any version except 1.2.3

# Hash checking for security (optional):
# package==1.2.3 \
#     --hash=sha256:abc123... \
#     --hash=sha256:def456...

# Generate hashes with:
# uv pip hash package-1.2.3.tar.gz

# To install with hash checking:
# uv pip install -r requirements.txt --require-hashes